services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api-ihm.keqing.icu/v1}
    ports:
      - "${PORT:-3000}:80"
    restart: unless-stopped
